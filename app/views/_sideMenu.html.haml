.block.active
  %span.glyphicon.glyphicon-th.icon{:aria=>{:hidden => 'true'}}
  .label PRODUCTS
.block
  %span.glyphicon.glyphicon-user.icon{:aria=>{:hidden => 'true'}}
  .label PROFILE
.block
  %span.glyphicon.glyphicon-shopping-cart.icon{:aria=>{:hidden => 'true'}}
  .label ORDERS
.block
  %span.glyphicon.glyphicon-ok.icon{:aria=>{:hidden => 'true'}}
  .label APPROVALS
.block
  %span.glyphicon.glyphicon-file.icon{:aria=>{:hidden => 'true'}}
  .label QUOTATIONS
.block
  %span.glyphicon.glyphicon-bell.icon{:aria=>{:hidden => 'true'}}
  .label NOTIFICATIONS
.block
  %span.glyphicon.glyphicon-credit-card.icon{:aria=>{:hidden => 'true'}}
  .label PAYMENTS